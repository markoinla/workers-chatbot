{
  "name": "chat-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for multi-tenant chat with AutoRAG",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "test": "vitest"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240129.0",
    "@ai-sdk/openai": "^0.0.66",
    "ai": "^3.4.32"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "wrangler": "^3.0.0"
  }
} 